[package]
name = "irondict-gui"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
irondict-core = { path = "../core" }
regex = "1.12.3"
slint = "1"
zbus = "5.16.0"

[build-dependencies]
slint-build = "1"
